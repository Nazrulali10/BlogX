import { Outfit,Lexend } from 'next/font/google'

export const outfit = Outfit({
  subsets: ['latin'], 
  display: 'swap',
});

export const lexend = Lexend({
  subsets: ['latin'], 
  display: 'swap',
});